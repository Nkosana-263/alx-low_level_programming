#include <stdio.h>

/**
 * main - a function containing the code to be executed, that
 * that prints exactly with proper grammar, but the outcome
 * is a piece of art,, followed by a new line
 * Return: 0 if the program executed successfully and did what
 * it was intended to do
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
